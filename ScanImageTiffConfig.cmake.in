@PACKAGE_INIT@
set(SCANIMAGETIFF_INCLUDE_DIR "${PACKAGE_PREFIX_DIR}/@CMAKE_INSTALL_INCLUDEDIR@")
set(SCANIMAGETIFF_LIB_DIR "${PACKAGE_PREFIX_DIR}/@CMAKE_INSTALL_LIBDIR@")

if(NOT SCANIMAGETIFF_FIND_QUIETLY)
  message(
    STATUS
      "Found SCANIMAGETIFF: ${SCANIMAGETIFF_INCLUDE_DIR}"
  )
endif()